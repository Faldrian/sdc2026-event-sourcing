spring:
  application:
    name: sdc2026-event-sourcing
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/eventstore
    username: postgres
    password: postgres
